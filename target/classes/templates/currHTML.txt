
		// .or(() -> candidateRepo.findByUsername(username))

	// 	return customerRepo.findByUsername(username)
	// 	.or(() -> bankMitraRepo.findByUsername(username))
	// 	.orElseThrow(() -> new UsernameNotFoundException("User not found"));
	// 